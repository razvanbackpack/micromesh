{
  "name": "micromesh",
  "private": true,
  "version": "0.2.2",
  "type": "module",
  "scripts": {
    "predev": "rimraf public/.vite/manifest.json",
    "dev": "vite",
    "prebuild": "rimraf public/js/",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "rimraf": "^5.0.7",
    "vite": "^5.2.0"
  }
}
